$('#my-posts').append('<%= j render @posts %>');
             <% if @posts.next_page %>
                  $('.pagination').replaceWith('<%= j will_paginate @posts %>');
             <% else %>
              $(window).off('scroll');
                 $('.pagination').remove();
             <% end %>

$('#my-topics').append('<%= j render @topics %>');
             <% if @posts.next_page %>
                  $('.pagination').replaceWith('<%= j will_paginate @topics %>');
             <% else %>
              $(window).off('scroll');
                 $('.pagination').remove();
             <% end %>